# Copy to .env.local and fill from the repo-root .env.
# Anon key only — the service-role key must never reach the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
