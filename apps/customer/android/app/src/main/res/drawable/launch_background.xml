<?xml version="1.0" encoding="utf-8"?>
<!--
  Pre-API-21 launch window. Flat brand orange, matching SplashPage, so the app
  opens into the splash rather than flashing white on the way to it.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/zopiq_splash_background" />
</layer-list>
