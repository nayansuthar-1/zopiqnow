<?xml version="1.0" encoding="utf-8"?>
<!--
    The expanded card: the collapsed one plus the line of prose the server sent
    ("Collecting your order from Kolkata Biryani House").

    Same tracker bitmap, same ids, so LiveCardNotification fills both views with
    one pair of calls and the two can never disagree about where the order is.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="2dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/zopiq_live_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textAppearance="@android:style/TextAppearance.Material.Notification.Title"/>

    <TextView
        android:id="@+id/zopiq_live_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="1dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:textAppearance="@android:style/TextAppearance.Material.Notification"/>

    <ImageView
        android:id="@+id/zopiq_live_tracker"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_marginTop="12dp"
        android:scaleType="fitXY"
        android:importantForAccessibility="no"/>

    <Chronometer
        android:id="@+id/zopiq_live_countdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:textAppearance="@android:style/TextAppearance.Material.Notification"/>

    <TextView
        android:id="@+id/zopiq_live_countdown_done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:visibility="gone"
        android:maxLines="1"
        android:ellipsize="end"
        android:textAppearance="@android:style/TextAppearance.Material.Notification"/>

</LinearLayout>
