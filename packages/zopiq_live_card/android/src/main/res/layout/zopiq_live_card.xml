<?xml version="1.0" encoding="utf-8"?>
<!--
    The collapsed card: what is on the lock screen at a glance.

    Only the body. The icon, app name, time and "Arriving in N min" sub-text along
    the top are the system's, supplied by DecoratedCustomViewStyle — which is why
    this file is two views rather than a whole notification, and why it inherits
    the shade's own light/dark theme instead of guessing at colours.

    Text appearances are the framework's notification styles, so the title reads
    at the weight and colour every other notification on the phone uses.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="2dp"
    android:paddingBottom="6dp">

    <TextView
        android:id="@+id/zopiq_live_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textAppearance="@android:style/TextAppearance.Material.Notification.Title"/>

    <ImageView
        android:id="@+id/zopiq_live_tracker"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:importantForAccessibility="no"/>

</LinearLayout>
