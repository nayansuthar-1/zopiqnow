<?xml version="1.0" encoding="utf-8"?>
<!--
    Collapsed live card body.

    Android's large-icon slot crops artwork into a small circle, so the food or
    rider image lives inside this custom body instead. The system still owns the
    notification header through DecoratedCustomViewStyle.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="2dp"
    android:paddingBottom="6dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/zopiq_live_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textAppearance="@android:style/TextAppearance.Material.Notification.Title"/>

        <ImageView
            android:id="@+id/zopiq_live_tracker"
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:layout_marginTop="8dp"
            android:scaleType="fitXY"
            android:importantForAccessibility="no"/>

        <Chronometer
            android:id="@+id/zopiq_live_countdown"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:textAppearance="@android:style/TextAppearance.Material.Notification"/>

        <TextView
            android:id="@+id/zopiq_live_countdown_done"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:visibility="gone"
            android:maxLines="1"
            android:ellipsize="end"
            android:textAppearance="@android:style/TextAppearance.Material.Notification"/>

    </LinearLayout>

    <ImageView
        android:id="@+id/zopiq_live_artwork"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_marginStart="12dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:importantForAccessibility="no"/>

</LinearLayout>
